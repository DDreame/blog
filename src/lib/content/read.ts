// TODO (#t8): Replace with actual implementation from [阶段2] 2.1b Markdown 文件读取工具
// This stub exists only because next build requires module resolution.
// When #t8 merges, delete this file — the real implementation will be merged.
import type { ContentType } from '@/lib/content-types'

export type { ContentFile } from '@/lib/content-types'

export function getPublicContent(_type: ContentType): unknown[] {
  return []
}

export function getAllContent(_type: ContentType): unknown[] {
  return []
}
